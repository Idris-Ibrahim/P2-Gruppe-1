extends layout

block content
    include nav
    h1= current = ''
    body
        button.dropbtn Sort
            .dropdown-content

                a(href='/events/sortddateasc') Date  &uarr;

                a(href='/events/sortddatedesc') Date &darr;

        ol

            .event-container

                each data in eventlist, grouplist
                        p()
                        a(href=`/eventsinfo/?id=${event.id}` style='color: white' method = 'GET' id=`EventInfo`)  #{event.event_name}
                            p()

                            .event
                                .event-left
                                    .event-date

                                .event-right

                                    .klub Klub: #{group_id}
                                    .date d. #{event.dato}
                                    .sted Sted: #{event.lokation}
                                    .event-timing Kl. #{event.tid}
                                    -if(
                                        .pris #{event.pris} kr.



    include pagination